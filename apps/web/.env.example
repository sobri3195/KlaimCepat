# API Configuration
# Uncomment and set this if you have a separate backend API
# VITE_API_URL=https://your-api-url.com/api/v1

# For local development (default proxy to localhost:3001)
# VITE_API_URL=http://localhost:3001/api/v1

# For Netlify deployment with backend proxy via netlify.toml
# Leave this commented out to use relative URL /api/v1
